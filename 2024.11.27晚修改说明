2024.11.27晚修改说明  （原代码均没有删除，而是注释了起来，并在旁边贴上新代码，方便找茬）
1.将函数PrintWood中形参number类型从char改为int，并新增了一个参数e，用来判断要打印的wood是否为空，如果为空则putchar('X')。并将函数movetoleft和movetoright中调用的PrintWood做了相应修改

2.在initialization.h中添加了结构体变量current_coord, 用来记录机器人手中木块的左上角的坐标， 便于之后使用。并在movetoleft和movetoright对current_coord进行了记录

3.删除了initialization.h中的input和output数组，统一使用instructs.h中定义的input（输入传送带）、output（输出传送带）、space（空地）矢量，并对initialization.cpp中涉及到的部分做了相应修改

但是好像还有一点小错误，我明天找时间修正一下...
